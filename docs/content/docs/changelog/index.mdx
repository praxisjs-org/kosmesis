---
title: Changelog
description: Release history for kosmesis.
icon: History
---

import { Card, Cards } from 'fumadocs-ui/components/card';

`kosmesis` (the CLI) is the only package in this repository published to npm — everything else
(`@kosmesis/registry`, `@kosmesis/docs`, `kosmesis-stories`) is private workspace tooling that
supports it, not something you install.

<Cards>
  <Card title="kosmesis" href="/docs/changelog/cli" description="The CLI — init and add commands, the registry, and everything you install." />
</Cards>
