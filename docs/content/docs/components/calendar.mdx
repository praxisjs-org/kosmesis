---
title: Calendar
description: A month-grid date picker with real date math. Purely presentational — no Morphos equivalent.
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="calendar" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="calendar" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-calendar--default" title="Default" height={600} />
    <StorybookEmbed story="tailwind-calendar--with-selection" title="With initial selection" height={600} />
    <StorybookEmbed story="tailwind-calendar--disabled-weekends" title="Disabled weekends" height={600} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-calendar--default" title="Default" height={600} />
    <StorybookEmbed story="praxiscss-calendar--with-selection" title="With initial selection" height={600} />
    <StorybookEmbed story="praxiscss-calendar--disabled-weekends" title="Disabled weekends" height={600} />
  </Tab>
</Tabs>

## Usage

```tsx
import { Calendar, CalendarState } from "@/components/ui/calendar";

@Component()
class BookingCalendar extends StatefulComponent {
  @State() calendarState = new CalendarState({
    onSelect: (date) => console.log(date),
  });

  render() {
    return <Calendar state={this.calendarState} />;
  }
}
```

## Props (`CalendarState`)

| Prop | Type | Default |
|---|---|---|
| `defaultMonth` | `Date` | today |
| `selected` | `Date` | — (controlled) |
| `onSelect` | `(date: Date) => void` | — |
| `disabled` | `(date: Date) => boolean` | — |

For a Popover-triggered variant, see [Date Picker](/docs/components/date-picker).
