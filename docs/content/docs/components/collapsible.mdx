---
title: Collapsible
description: A single expand/collapse toggle, wrapping Morphos's Disclosure primitive.
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="collapsible" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="collapsible" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-collapsible--default" title="Default" height={460} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-collapsible--default" title="Default" height={460} />
  </Tab>
</Tabs>

## Usage

```tsx
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible";

@Component()
class Notes extends StatefulComponent {
  @State() disclosure = new Collapsible();

  render() {
    return (
      <Collapsible class="w-96">
        <CollapsibleTrigger disclosure={this.disclosure}>Toggle notes</CollapsibleTrigger>
        <CollapsibleContent disclosure={this.disclosure}>
          <p>These are the hidden notes.</p>
        </CollapsibleContent>
      </Collapsible>
    );
  }
}
```
