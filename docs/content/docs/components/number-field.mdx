---
title: Number Field
description: A decrement/input/increment stepper, wrapping Morphos's NumberField primitive.
icon: Hash
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="number-field" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="number-field" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-numberfield--default" title="Default" height={380} />
    <StorybookEmbed story="tailwind-numberfield--controlled" title="Controlled" height={380} />
    <StorybookEmbed story="tailwind-numberfield--with-step" title="Step of 0.5" height={380} />
    <StorybookEmbed story="tailwind-numberfield--currency" title="Currency formatting" height={380} />
    <StorybookEmbed story="tailwind-numberfield--disabled" title="Disabled" height={380} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-numberfield--default" title="Default" height={380} />
    <StorybookEmbed story="praxiscss-numberfield--controlled" title="Controlled" height={380} />
    <StorybookEmbed story="praxiscss-numberfield--with-step" title="Step of 0.5" height={380} />
    <StorybookEmbed story="praxiscss-numberfield--currency" title="Currency formatting" height={380} />
    <StorybookEmbed story="praxiscss-numberfield--disabled" title="Disabled" height={380} />
  </Tab>
</Tabs>

## Usage

```tsx
import { NumberField } from "@/components/ui/number-field";

<NumberField defaultValue={3} min={0} max={10} step={1} aria-label="Quantity" />
```

### Formatting

`formatOptions` takes an `Intl.NumberFormat` options object — the displayed text is formatted with
it, while the underlying value stays a plain `number`:

```tsx
<NumberField
  defaultValue={19.99}
  min={0}
  step={0.01}
  formatOptions={{ style: "currency", currency: "USD" }}
  aria-label="Price"
/>
```

## Props

Passes through to Morphos's `NumberField`.

| Prop | Type | Default |
|---|---|---|
| `value` | `number` | — controlled value |
| `defaultValue` | `number` | — |
| `min` | `number` | — |
| `max` | `number` | — |
| `step` | `number` | — |
| `disabled` | `boolean` | — |
| `required` | `boolean` | — |
| `name` | `string` | — |
| `formatOptions` | `Intl.NumberFormatOptions` | — e.g. `{ style: "currency", currency: "USD" }` |
| `onValueChange` | `(value: number) => void` | — |

The underlying input is `type="text"` with `inputmode="decimal"` — not `type="number"` — so no
browser-native spinner UI ever shows through; the decrement/increment buttons are the only way to
step the value with a pointer.
