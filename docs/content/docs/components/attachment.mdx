---
title: Attachment
description: A file/media attachment chip, typically used inside Message. Purely presentational.
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="attachment" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="attachment" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-attachment--default" title="Default" height={380} />
    <StorybookEmbed story="tailwind-attachment--removable" title="Removable" height={380} />
    <StorybookEmbed story="tailwind-attachment--group" title="Group" height={380} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-attachment--default" title="Default" height={380} />
    <StorybookEmbed story="praxiscss-attachment--removable" title="Removable" height={380} />
    <StorybookEmbed story="praxiscss-attachment--group" title="Group" height={380} />
  </Tab>
</Tabs>

## Usage

```tsx
import { Attachment, AttachmentGroup } from "@/components/ui/attachment";

<AttachmentGroup>
  <Attachment name="invoice.pdf" size="248 KB" onRemove={() => {}} />
  <Attachment name="photo.png" size="1.2 MB" onRemove={() => {}} />
</AttachmentGroup>
```

## Props

### Attachment

| Prop | Type | Default |
|---|---|---|
| `name` | `string` | — required |
| `size` | `string` | — display-only size label (e.g. `"248 KB"`) |
| `onRemove` | `() => void` | — when set, renders a remove (✕) button |
| `children` | `Children` | `"📎"` — overrides the default icon slot |

`AttachmentGroup` takes no props beyond `class`/`children`.
