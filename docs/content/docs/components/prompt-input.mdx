---
title: Prompt Input
description: A chat composer — auto-resizing textarea + submit button + a toolbar slot. Purely presentational — no Morphos equivalent.
icon: SendHorizontal
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="prompt-input" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="prompt-input" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-prompt-input--default" title="Default" height={140} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-prompt-input--default" title="Default" height={140} />
  </Tab>
</Tabs>

## About

Purely presentational — no Morphos equivalent. `value` behaves like `CalendarState.selected`:
pass a plain string for uncontrolled use, or a getter function to stay controlled. Enter submits,
Shift+Enter inserts a newline; `children` render as a toolbar row below the textarea (attachments,
model pickers, ...).

## Usage

```tsx
import { Icon } from "@morphos/icons";

import { PromptInput } from "@/components/ui/prompt-input";

<PromptInput placeholder="Ask anything..." onSubmit={(value) => sendMessage(value)}>
  <Button variant="ghost" size="sm">
    <Icon name="Paperclip" size={16} />
  </Button>
</PromptInput>
```

## Props

| Prop | Type | Default |
|---|---|---|
| `value` | `string` | — (uncontrolled when omitted) |
| `defaultValue` | `string` | `""` |
| `placeholder` | `string` | `"Ask anything..."` |
| `disabled` | `boolean` | `false` |
| `onChange` | `(value: string) => void` | — |
| `onSubmit` | `(value: string) => void` | — |
| `class` | `string` | — |
