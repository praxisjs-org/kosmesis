---
title: Toast
description: The classic toast API — a thin re-export of Sonner (same underlying Morphos ToastProvider).
icon: Bell
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="toast" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="toast" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-toast--default" title="Default" height={380} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-toast--default" title="Default" height={380} />
  </Tab>
</Tabs>

<Callout type="info">
  shadcn/ui documents `Toast` (older, Radix-backed) and `Sonner` (modern, standalone-package-backed)
  separately, because upstream they're genuinely different libraries. Morphos has a single
  `ToastProvider`/`Toast` primitive behind both concepts, so this page — and its registry entry —
  is a plain re-export of [Sonner](/docs/components/sonner). Use whichever import path you prefer;
  they're the same `Toaster` and `toast()`.
</Callout>

```tsx
import { Toaster, toast } from "@/components/ui/toast";
// identical to:
import { Toaster, toast } from "@/components/ui/sonner";
```
