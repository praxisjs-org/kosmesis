---
title: Animated List
description: A list whose items fade/slide in with a per-item stagger delay. Purely presentational — no Morphos equivalent.
icon: ListTree
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="animated-list" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="animated-list" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-animated-list--default" title="Default" height={260} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-animated-list--default" title="Default" height={260} />
  </Tab>
</Tabs>

## About

Purely presentational — no Morphos equivalent. The Tailwind flavor reuses `tw-animate-css`'s
`animate-in`/`fade-in`/`slide-in-from-top` utilities (already a Kosmesis runtime dependency, see
`Dialog`/`Popover`); the `@praxisjs/css` flavor defines the same fade+slide as a local `keyframes()`.
Either way, the per-item stagger delay (`index * 80ms`) is a dynamic value applied via inline style.

## Usage

```tsx
import { AnimatedList, AnimatedListItem } from "@/components/ui/animated-list";

<AnimatedList>
  {items.map((item, i) => (
    <AnimatedListItem key={item.id} index={i}>{item.text}</AnimatedListItem>
  ))}
</AnimatedList>
```

## Props

### AnimatedListItem

| Prop | Type | Default |
|---|---|---|
| `index` | `number` | `0` |
| `class` | `string` | — |

`AnimatedList` accepts `class`, `id`, and `children` only.
