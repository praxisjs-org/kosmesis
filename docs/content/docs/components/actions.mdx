---
title: Actions
description: A row of small icon action buttons under an AI response. Purely presentational — no Morphos equivalent.
icon: EllipsisVertical
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="actions" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="actions" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-actions--default" title="Default" height={80} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-actions--default" title="Default" height={80} />
  </Tab>
</Tabs>

## About

Purely presentational — no Morphos equivalent. `Actions` is the row container; each `Action`
is a plain icon button (copy, regenerate, share, ...). Pairs well with `Message`'s `actions` slot.

## Usage

```tsx
import { Icon } from "@morphos/icons";

import { Action, Actions } from "@/components/ui/actions";

<Actions>
  <Action label="Copy" onClick={copyText}><Icon name="Copy" size={16} /></Action>
  <Action label="Regenerate" onClick={regenerate}><Icon name="RotateCw" size={16} /></Action>
</Actions>
```

## Props

### Action

| Prop | Type | Default |
|---|---|---|
| `label` | `string` | — |
| `onClick` | `() => void` | — |
| `class` | `string` | — |

`Actions` accepts `class` and `children` only.
