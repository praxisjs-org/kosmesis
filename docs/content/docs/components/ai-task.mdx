---
title: AI Task
description: A single collapsible agent task card with a live status pill. Purely presentational — no Morphos equivalent.
icon: ListTodo
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="ai-task" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="ai-task" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-ai-task--default" title="Default" height={160} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-ai-task--default" title="Default" height={160} />
  </Tab>
</Tabs>

## About

Purely presentational — no Morphos equivalent. Distinct from `AiSteps` (a flat non-collapsible
list of many steps) and `ChainOfThought` (reasoning text, not task status): this is one named task
whose `status` is expected to change live as it runs.

## Usage

```tsx
import { AiTask } from "@/components/ui/ai-task";

<AiTask title="Refactor auth module" status="running" defaultOpen>
  <span>src/auth/login.ts</span>
  <span>src/auth/session.ts</span>
</AiTask>
```

## Props

| Prop | Type | Default |
|---|---|---|
| `title` | `string` | — |
| `status` | `"pending" \| "running" \| "done" \| "error"` | `"pending"` |
| `defaultOpen` | `boolean` | `false` |
| `class` | `string` | — |
