---
title: Lens
description: A magnifying-glass image zoom that follows the pointer. Purely presentational — no Morphos equivalent.
icon: ZoomIn
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="lens" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="lens" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-lens--default" title="Default" height={280} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-lens--default" title="Default" height={280} />
  </Tab>
</Tabs>

## About

Purely presentational — no Morphos equivalent. The lens is the same image as a
`background-image`, sized to `containerSize * zoom` and offset so the point under the cursor stays
centered under it — recomputed on every pointer move since the container can be responsive
(written imperatively rather than through `@State`, same reasoning as `Resizable`).

## Usage

```tsx
import { Lens } from "@/components/ui/lens";

<Lens class="h-64 w-full rounded-lg" src="/product-photo.jpg" alt="Product" zoom={2} lensSize={150} />
```

## Props

| Prop | Type | Default |
|---|---|---|
| `src` | `string` | — |
| `alt` | `string` | `""` |
| `zoom` | `number` | `2` |
| `lensSize` | `number` (px diameter) | `150` |
| `class` | `string` | — |
