---
title: AI Branch
description: Navigable alternate responses (regenerations) for one AI turn. Purely presentational — no Morphos equivalent.
icon: GitBranch
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="ai-branch" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="ai-branch" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-ai-branch--default" title="Default" height={140} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-ai-branch--default" title="Default" height={140} />
  </Tab>
</Tabs>

## About

Purely presentational — no Morphos equivalent. Every branch is rendered up front and toggled via a
`data-active` attribute (not swapped in/out), the same pattern `StickyScroll`'s panel uses, since a
reactive children-thunk can't return a bare `Children` value. The prev/next nav only renders when
there's more than one branch.

## Usage

```tsx
import { AiBranch } from "@/components/ui/ai-branch";

<AiBranch branches={[responseA, responseB, responseC]} />
```

## Props

| Prop | Type | Default |
|---|---|---|
| `branches` | `Children[]` | — |
| `class` | `string` | — |
