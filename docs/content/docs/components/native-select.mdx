---
title: Native Select
description: A plain, styled native <select>. Purely presentational.
icon: List
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Callout } from 'fumadocs-ui/components/callout';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="native-select" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="native-select" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-nativeselect--default" title="Default" height={380} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-nativeselect--default" title="Default" height={380} />
  </Tab>
</Tabs>

## Usage

```tsx
import { NativeSelect } from "@/components/ui/native-select";

<NativeSelect defaultValue="apple" onChange={(value) => console.log(value)}>
  <option value="apple">Apple</option>
  <option value="banana">Banana</option>
  <option value="cherry">Cherry</option>
</NativeSelect>
```

<Callout type="info">
  Prefer [Select](/docs/components/select) for a fully custom-styleable dropdown; reach for
  `NativeSelect` specifically when you want native OS picker behavior (better on mobile, or for
  very long option lists where a custom listbox would need virtualization).
</Callout>

## Props

| Prop | Type | Default |
|---|---|---|
| `value` | `string` | — controlled value |
| `defaultValue` | `string` | — |
| `disabled` | `boolean` | — |
| `required` | `boolean` | — |
| `name` | `string` | — |
| `onChange` | `(value: string, event: Event) => void` | — |
