---
title: Textarea
description: A multi-line text input. Purely presentational — no Morphos primitive (Radix has none either).
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="textarea" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="textarea" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-textarea--default" title="Default" height={380} />
    <StorybookEmbed story="tailwind-textarea--with-default-value" title="With default value" height={380} />
    <StorybookEmbed story="tailwind-textarea--disabled" title="Disabled" height={380} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-textarea--default" title="Default" height={380} />
    <StorybookEmbed story="praxiscss-textarea--with-default-value" title="With default value" height={380} />
    <StorybookEmbed story="praxiscss-textarea--disabled" title="Disabled" height={380} />
  </Tab>
</Tabs>

## Usage

```tsx
import { Textarea } from "@/components/ui/textarea";

<Textarea placeholder="Type your message here." rows={4} />
```

## Props

| Prop | Type | Default |
|---|---|---|
| `value` / `defaultValue` | `string` | — |
| `rows` | `number` | `3` |
| `disabled` / `readonly` / `required` | `boolean` | — |
| `onInput` / `onChange` | `(value: string, event: Event) => void` | — |
