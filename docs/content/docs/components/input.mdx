---
title: Input
description: A single-line text input, wrapping Morphos's Input primitive.
icon: TextCursorInput
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="input" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="input" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-input--default" title="Default" height={380} />
    <StorybookEmbed story="tailwind-input--disabled" title="Disabled" height={380} />
    <StorybookEmbed story="tailwind-input--invalid" title="Invalid" height={380} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-input--default" title="Default" height={380} />
    <StorybookEmbed story="praxiscss-input--disabled" title="Disabled" height={380} />
    <StorybookEmbed story="praxiscss-input--invalid" title="Invalid" height={380} />
  </Tab>
</Tabs>

## Usage

```tsx
import { Input } from "@/components/ui/input";

<Input type="email" placeholder="you@example.com" />
<Input disabled placeholder="Disabled" />
<Input invalid placeholder="Invalid" />
```

## Props

Passes through to Morphos's `Input`. See
[Morphos's Input docs](https://morphos.praxisjs.org/docs/inputs/input) for details.

| Prop | Type | Default |
|---|---|---|
| `type` | `"text" \| "email" \| "password" \| "number" \| "search" \| "tel" \| "url"` | — |
| `value` | `string` | — |
| `defaultValue` | `string` | — |
| `placeholder` | `string` | — |
| `disabled` | `boolean` | — |
| `readonly` | `boolean` | — |
| `required` | `boolean` | — |
| `invalid` | `boolean` | — |
| `name` | `string` | — |
| `autoComplete` | `string` | — |
| `maxLength` | `number` | — |
| `minLength` | `number` | — |
| `onInput` | `(value: string, event: Event) => void` | — |
| `onChange` | `(value: string, event: Event) => void` | — |
| `onFocus` | `(event: FocusEvent) => void` | — |
| `onBlur` | `(event: FocusEvent) => void` | — |
| `aria-label` | `string` | — |
| `aria-labelledby` | `string` | — |
| `aria-describedby` | `string` | — |
