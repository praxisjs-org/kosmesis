---
title: Chain of Thought
description: A collapsible list of an AI response's named reasoning steps. Purely presentational — no Morphos equivalent.
icon: Workflow
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="chain-of-thought" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="chain-of-thought" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-chain-of-thought--default" title="Default" height={200} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-chain-of-thought--default" title="Default" height={200} />
  </Tab>
</Tabs>

## About

Purely presentational — no Morphos equivalent. Distinct from `Reasoning` (a single collapsible
text block): this is a stepped list of named steps, each with its own `status`.

## Usage

```tsx
import { ChainOfThought, ChainOfThoughtStep } from "@/components/ui/chain-of-thought";

<ChainOfThought>
  <ChainOfThoughtStep status="complete">Searched the docs for "refund policy"</ChainOfThoughtStep>
  <ChainOfThoughtStep status="active">Drafting a response</ChainOfThoughtStep>
  <ChainOfThoughtStep status="pending">Reviewing tone</ChainOfThoughtStep>
</ChainOfThought>
```

## Props

### ChainOfThought

| Prop | Type | Default |
|---|---|---|
| `defaultOpen` | `boolean` | `true` |
| `class` | `string` | — |

### ChainOfThoughtStep

| Prop | Type | Default |
|---|---|---|
| `status` | `"complete" \| "active" \| "pending"` | `"complete"` |
| `class` | `string` | — |
