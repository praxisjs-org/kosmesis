---
title: Message
description: Composes Bubble with an optional avatar and role-based layout for chat UIs.
icon: MessageSquare
---

import { KosmesisInstall } from '@/components/kosmesis-install';
import { ManualInstall } from '@/components/manual-install';
import { SlimTabs, SlimTab } from '@/components/tabs';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { StorybookEmbed } from '@/components/storybook-embed';

## Installation

<SlimTabs items={['CLI', 'Manual']}>
  <SlimTab value="CLI">
    <KosmesisInstall name="message" />
  </SlimTab>
  <SlimTab value="Manual">
    <ManualInstall name="message" />
  </SlimTab>
</SlimTabs>

## Examples

<Tabs items={['Tailwind', 'PraxisCSS']}>
  <Tab value="Tailwind">
    <StorybookEmbed story="tailwind-message--default" title="Default" height={380} />
    <StorybookEmbed story="tailwind-message--conversation" title="Conversation" height={380} />
  </Tab>
  <Tab value="PraxisCSS">
    <StorybookEmbed story="praxiscss-message--default" title="Default" height={380} />
    <StorybookEmbed story="praxiscss-message--conversation" title="Conversation" height={380} />
  </Tab>
</Tabs>

## Usage

```tsx
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Message, MessageGroup } from "@/components/ui/message";

@Component()
class Chat extends StatefulComponent {
  @State() avatar = new Avatar();

  render() {
    return (
      <MessageGroup>
        <Message from="assistant" avatar={
          <Avatar class="size-6"><AvatarFallback avatar={this.avatar}>AI</AvatarFallback></Avatar>
        }>
          How can I help you today?
        </Message>
        <Message from="user">What's the weather like?</Message>
      </MessageGroup>
    );
  }
}
```

Use with [Message Scroller](/docs/components/message-scroller) for a scrollable chat viewport, and
[Attachment](/docs/components/attachment) for file previews within a message.

## Props

### Message

| Prop | Type | Default |
|---|---|---|
| `from` | `"user" \| "assistant"` | — required |
| `avatar` | `Children` | — rendered before the bubble (e.g. an `Avatar`) |

`MessageGroup` takes no props beyond `class`/`children`.
